<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.biitautomatatutor">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BiitAutomataTutor">
        <activity android:name=".SavedGraphShow"></activity>
        <activity android:name=".SavedGraph" />
        <activity android:name=".MooreTrySelf" />
        <activity android:name=".MooreTutorial" />
        <activity android:name=".MealyTrySelf" />
        <activity android:name=".MealyTutorial" />
        <activity android:name=".NfaTrySelf" />
        <activity android:name=".regextrySelf" />
        <activity android:name=".RegularExpresssion" />
        <activity android:name=".DfaTrySelf">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".DfaTutorial" />
        <activity android:name=".NfaTutorial" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>